{
  "name": "resume",
  "version": "0.2.0",
  "private": true,
  "homepage": "http://thuydao.github.io/cv",
  "dependencies": {
    "punycode": "^2.3.1",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider react-scripts build && NODE_OPTIONS=--openssl-legacy-provider react-scripts start",
    "build": "NODE_OPTIONS=--openssl-legacy-provider react-scripts react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "NODE_OPTIONS=--openssl-legacy-provider gh-pages -d build"
  },
  "devDependencies": {
    "ajv": "^6.5.4",
    "gh-pages": "^2.2.0",
    "url-parse": "^1.4.3"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
